<UserControl x:Class="unBand.pages.ActivityLogPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:unBand"
             mc:Ignorable="d" Width="672" Height="412">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <CheckBox Margin="5">Steps</CheckBox>
                <CheckBox Margin="5">Sleep</CheckBox>
                <CheckBox Margin="5">Workouts</CheckBox>
                <CheckBox Margin="5">Runs</CheckBox>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Margin="5" TextWrapping="Wrap">We're working on date ranges, but in the meanwhile you can:</TextBlock>
                <RadioButton Margin="5">Export Last 100 Activities</RadioButton>
                <RadioButton Margin="5">Export All</RadioButton>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <StackPanel Margin="5" Orientation="Horizontal">
                    <TextBlock Height="15">Format:</TextBlock>
                    <ComboBox Margin="5" HorizontalAlignment="Left">
                        <ComboBoxItem Selector.IsSelected="True">CSV</ComboBoxItem>
                        <ComboBoxItem>Excel</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
                <Button Margin="10" Click="btnExport_Click">Export</Button>
            </StackPanel>

            <StackPanel Orientation="Vertical" Visibility="Collapsed">
                <Button Margin="5,5,5,189" Content="Export Last 100 Activities" Click="btnExportLast100_Click" Width="167"></Button>
                <Button Margin="5,5,5,189" Content="Export All Activities" Click="btnExportAll_Click" Width="164"/>
            </StackPanel>

        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding Events}"></ListBox>

    </Grid>
</UserControl>
