<UserControl x:Class="unBand.pages.MyBandPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:unBand"
             xmlns:BandControls="clr-namespace:unBand.Controls"
             mc:Ignorable="d" d:DesignWidth="300" Height="264">

    <Grid>
        <Grid Visibility="{Binding IsConnected, ConverterParameter=true, Converter={StaticResource BoolToVisibilityConverter}}" local:DesignHelpers.IsHidden="true">
            <TextBlock TextWrapping="Wrap">No Microsoft Band connected! Connect your Band to your computer with the USB charging cable.</TextBlock>
        </Grid>

        <Grid Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="120"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <BandControls:BandMeTile Grid.Row="0" MouseUp="BandMeTile_MouseUp"></BandControls:BandMeTile>
            
        </Grid>
    </Grid>
</UserControl>
